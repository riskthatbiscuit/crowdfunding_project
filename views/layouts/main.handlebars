<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fund My Project!</title>
    {{!-- <link rel="stylesheet" href="/css/jass.css"> --}}
    {{!-- <link rel="stylesheet" href="/css/style.css"> --}}
    <script src="https://cdn.tailwindcss.com"></script>
</head>


<body>
    
    <header>
        <nav class="mx-auto flex max-w-7xl items-center justify-between p-6 lg:px- bg-black text-slate-50" aria-label="Global">
            <div class="flex lg:flex-1">
                <a href="/" class="-m-1.5 p-1.5">
                    <span class="text-xt font-bold text-2xl">💸  Fund My Project</span>
                </a>
            </div>
            <div class="flex lg:hidden">
                <button type="button" class="-m-2.5 inline-flex items-center justify-center rounded-md p-2.5 text-gray-700">
                    <span class="sr-only">Open main menu</span>
                    <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"
                        aria-hidden="true">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" />
                    </svg>
                </button>
            </div>
            <div>
                {{#if logged_in}}
                <a href="/profile" class="text-sm font-semibold leading-6 text-slate-50">Profile</a> |
                <button class="no-button text-sm font-semibold leading-6 text-slate-50" id="logout">Log out</button>
                {{else}}
                <a href="/login"
                    class="text-sm font-semibold leading-6 text-slate-50">Log in <span aria-hidden="true">&rarr;</span></a>
                {{/if}}
            </div>
        </nav>
    </header>

    <main class="mx-auto flex max-w-7xl align-center justify-center p-6 lg:px-8">
        {{{body}}}
    </main>

        <footer>
            
        </footer>

    </div class="py-6"> 
    {{#if logged_in}}
    <script src="./js/logout.js"></script>
    {{/if}}
</body>
</html>